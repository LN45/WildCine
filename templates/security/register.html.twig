{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <div class="container register_container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-lg mt-5 button_connexion">
            Sign in
        </button>
        {{ form_end(form) }}
    </div>
{% endblock %}
